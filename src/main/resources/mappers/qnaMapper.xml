<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boo.mappers.qnaMapper">

	<!-- qna 목록 -->
	<select id="list" resultType="com.board.dto.QnaDto">
		select
			bno, title, content, writer, regDate, viewCnt
		from qna_board
	</select>
	
	<!-- qna 작성 -->
	<insert id="write" parameterType="com.board.dto.QnaDto">
		insert into
			qna_board(title, content, writer)
				values(#{title}, #{content}, #{writer})
	</insert>
	
	<!-- qna 조회 -->
	<select id="view" parameterType="int" resultType="com.board.dto.QnaDto">
		select
			bno, title, content, writer, regDate, viewCnt
		from
			qna_board
		where
			bno = #{bno}
	</select>
	
	<!-- qna 수정 -->
	<update id="modify" parameterType="com.board.dto.QnaDto">
		update qna_board
			set
				title = #{title},
				content = #{content},
				writer = #{writer}
			where bno = #{bno}
	</update>
	
	<!-- qna 삭제 -->
	<delete id="delete" parameterType="int">
		delete 
			from qna_board
		where bno = #{bno}
	</delete>
	
	<!-- qna 총 갯수 -->
	<select id="count" resultType="int">
		select count(bno) from qna_board
	</select>
	
	<!-- qna 목록 + 페이징 -->
	<select id="listPage" parameterType="hashMap" resultType="com.board.dto.QnaDto">
		select
			bno, title, writer, regDate, viewCnt
		from qna_board
		order by bno desc
			limit #{displayPost}, #{postNum}
	</select>
	
</mapper>